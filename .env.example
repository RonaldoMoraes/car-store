# Local Postgres (docker compose up -d)
DATABASE_URL=postgres://paperclip:paperclip@localhost:5433/paperclip

# Root domain the platform serves tenant subdomains from (local dev uses *.localhost)
PLATFORM_ROOT_DOMAIN=localhost:3000

# Session signing secret (generate with: openssl rand -hex 32)
AUTH_SECRET=
